{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "framework": "fastapi",
  "buildCommand": "python3.13 -m pip install uv && uv pip install --system -r pyproject.toml",
  "installCommand": "python3.13 -m pip install uv && uv pip install --system -r pyproject.toml",
  "env": {
    "LOG_LEVEL": "INFO",
    "LOG_FILE_ENABLED": "false",
    "DATA_DIR": "/tmp/data",
    "PYTHON_VERSION": "3.13"
  }
}